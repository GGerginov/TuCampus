<UserControl x:Class="TuCampus.Views.Users.AddNewUserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label Content="Username:" Grid.Row="0" />
        <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" />
        <Label Content="Password:" Grid.Row="2" />
        <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" />
        <Label Content="Email:" Grid.Row="4" />
        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Grid.Row="5" />
        <Label Content="Faculty Number:" Grid.Row="6" />
        <TextBox Text="{Binding FacultyNumber, UpdateSourceTrigger=PropertyChanged}" Grid.Row="7" />
        <Button Content="Add New User" Command="{Binding AddNewUserCommand}" Grid.Row="8" HorizontalAlignment="Center" />
    </Grid>
</UserControl>
